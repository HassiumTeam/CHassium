
func fib(n) {
    if (n == 0) return n;
    if (n == 1) return n;
    return fib (n - 2) + fib (n - 1);
}

println (fib (8));

func main () {
    arr = [1, 2, 3];
    arr2 = arr.map(fn (n) { return n + 1; });
    foreach (i in arr2) {
        println(i);
    }
    obj = {
        arr,
        num: 5,
        "str": "s",
    };

    foreach (i in arr) {
        println (i);
    }


    a = [1, 2, 4];
    a [3] = 5;
    a [1] = 6;
    a [9] = 2;

    foreach (n in a) {
        println (n);
    }
}
main ();

