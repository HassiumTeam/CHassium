func main () {
    arr = [1, 2, 3];
    obj = {
        arr,
        num: 5,
        "str": "s",
    };

    foreach (i in arr) {
        println (i);
    }

    println (obj.arr [2]);
    obj.num += 2;
    println (obj.num);
    println (10.5);
    println (add(10, 20, obj.num));
    println (void ());

    anon = fn (x, y) {
        return x + y + obj.num;
    };

    println (anon (40, 2));
}

func add (x, y, z) {
    return x + y + z;
}

func void () {
    
}

main ();
