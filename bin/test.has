class TestStats {
    func new () {
        this.passed = 0;
        this.failed = 0;
    }

    func toString () {
        return "Passed: " + this.passed;
    }
}

stats = TestStats ();
main ();

func main () {
    println (stats.passed);
    println (stats);
}
